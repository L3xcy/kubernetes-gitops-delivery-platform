apiVersion: v1
kind: Service
metadata:
  name: {{ include "delivery-demo.fullname" . }}
  labels:
    {{- include "delivery-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "delivery-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
